[pytest]
# Pytest configuration file
# This ensures pytest can find the project modules

# Add the project root to Python path
pythonpath = .

# Test discovery patterns
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers
